<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nl.alledaags.lightcurbsdktestapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".StartApplication">

        <activity android:name="nl.alledaags.lightcurbsdktestapp.activity.MainActivity"
            android:label="LightcurbSDK Android">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="nl.alledaags.lightcurbsdktestapp.activity.DetailActivity">

        </activity>

        <service
            android:name="nl.alledaags.lightcurbsdktestapp.service.NotificationService">
        </service>

    </application>

</manifest>
